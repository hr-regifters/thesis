var HomePage(props) => (
  if (this.state.view === 'home') {
    return (
      <div>
      </div>
    )
    
  } else if (this.state === 'concoction') {
    //use the spotlightConcoctionId to find the correct concoction in the concoctions array
    //build the concoction view out of the info stored in the object
    return (
      <div>
      </div>
    )

  }
)